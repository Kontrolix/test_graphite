pull_request_rules:
  - name: Merge on approvalllllllsss
    conditions:
      - label=Approved
      - base=main
    actions:
      queue:
        name: default

queue_rules:
  - name: default
    conditions:
      - base=main
