pull_request_rules:
  - name: merge on label
    conditions:
      - label=merge
      - base=main
    actions:
      queue:
        name: default

queue_rules:
  - name: default
    conditions:
      - base=main
