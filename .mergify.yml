pull_request_rules:
  - name: Merge on approvall
    conditions:
      - label=Approved
      - base=main
    actions:
      queue:
        name: default

queue_rules:
  - name: default
    conditions:
      - base=main
